test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# AWS S3 — production (set AWS_* in .env).
amazon:
  service: S3
  access_key_id: <%= ENV['AWS_ACCESS_KEY_ID'] %>
  secret_access_key: <%= ENV['AWS_SECRET_ACCESS_KEY'] %>
  region: <%= ENV.fetch('AWS_REGION', 'us-east-1') %>
  bucket: <%= ENV['AWS_BUCKET'] %>

# MinIO — S3-compatible local/object storage (https://github.com/minio/minio).
minio:
  service: S3
  access_key_id: <%= ENV['MINIO_ACCESS_KEY_ID'] %>
  secret_access_key: <%= ENV['MINIO_SECRET_ACCESS_KEY'] %>
  region: <%= ENV.fetch('MINIO_REGION', 'us-east-1') %>
  bucket: <%= ENV.fetch('MINIO_BUCKET', 'helpdesk') %>
  endpoint: <%= ENV.fetch('MINIO_ENDPOINT', 'http://127.0.0.1:9000') %>
  force_path_style: true

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket

# Use rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
# microsoft:
#   service: AzureStorage
#   storage_account_name: your_account_name
#   storage_access_key: <%= Rails.application.credentials.dig(:azure_storage, :storage_access_key) %>
#   container: your_container_name

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
